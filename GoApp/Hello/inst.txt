Para compilar Go en un WA:
1. Me ubico en la carpeta desde el PowerShell
2. Se cambia el Go OS y el Go ARCH:
set GOOS=js
set GOARCH=wasm
3. Ejecuto el comando :
    go build -o web/app.wasm
Que me pasara los archivos a WA en la carpeta WEB